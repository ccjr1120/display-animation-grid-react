export const calcChildLayout = (layoutMap, cols) => {
  const layouts = Object.values(layoutMap);
  const seats: Array<Array<number>> = [];
  const positionLayout = [];
  for (let i = 0; i < layouts.length; i++) {
    const { w, h } = layouts[i];
    const wj = 0;
    while(true){
      const hj = 0;

    }
  }
};
